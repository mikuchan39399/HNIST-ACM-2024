# 模板现在测得怎么样

自动生成, 不手改. 仅总结已登记范围内的证据. 代码变化后请重新生成, 静态文件不会自行刷新.

[AI 验证明细](details.md)

| 模板 | 现在怎么样 | 已测内容 | 还要注意什么 |
|---|---|---|---|
| [seg](<../../algorithms/数据结构/线段树/泛型线段树.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 七组真实插件双引擎与每组20万规模; 稀疏零域适配按插件契约区分 / 区间代数、左右查找及 20 万规模已验; 重建前 init | 仅这七个常用插件, 不代表旧历史最值/取模等专题已验; Binary 与 Linear 必须 build; Xor 值与掩码在位宽内且长度<=INT_MAX; 其余普通整数运算及累积标记须在 LL 内 / 区间非空且合法, 重建前 init; find 是区间存在性判据, 不累积前缀; 势能复杂度由代数保证; 等差用例只向整域施加局部标记 |
| [dynamicSeg](<../../algorithms/数据结构/线段树/泛型动态线段树.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 七组真实插件双引擎与每组20万规模; 稀疏零域适配按插件契约区分 / 稀疏/完整树及 20 万操作已验; clear/init 和 build 自动清空可复用预算 | 仅这七个常用插件, 不代表旧历史最值/取模等专题已验; Binary 与 Linear 必须 build; Xor 值与掩码在位宽内且长度<=INT_MAX; 其余普通整数运算及累积标记须在 LL 内 / Info{} 补 len 必须表示初始零值区间; 查询/find 下传也可能开点; 正值域/合法区间, 数值运算不溢出; find 不累积前缀; 不验所有自定义代数及现有插件 |
| [persistentSeg](<../../algorithms/数据结构/线段树/可持久化线段树/主席树.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 历史分叉/多根第 k 小与 20 万版本已验, 查询和 find 零开点; 加法最值支持 | 只测加法与计数代数, 数值及中间运算不溢出; find 必须是存在性判据; kth 仅点修改计数且逐位置合成非负/k 合法; set_n/build 改值域后不得访问旧值域根; build 不清池 |
| [segSplit](<../../algorithms/数据结构/线段树/线段树合并分裂/线段树按值域分裂.cpp>) | 还没有登记具体测了哪些行为 | 尚未发现直接关联的套件 | 不能仅凭 include 或其他模板通过就判断它已测好 |
| [segSplitSort](<../../algorithms/数据结构/线段树/线段树合并分裂/线段树分裂解决区间排序后单点查询问题.cpp>) | 还没有登记具体测了哪些行为 | 尚未发现直接关联的套件 | 不能仅凭 include 或其他模板通过就判断它已测好 |
| [segMergeTree](<../../algorithms/数据结构/线段树/线段树合并分裂/线段树合并解决树上查分词频问题.cpp>) | 还没有登记具体测了哪些行为 | 尚未发现直接关联的套件 | 不能仅凭 include 或其他模板通过就判断它已测好 |
| [persistentLeftist](<../../algorithms/数据结构/堆/左偏树/可持久化左偏树.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 双堆型 20 万版本与历史不可变性已验, 明确原地合并的独占条件 | 所有旧根在 init 后停用; merge_raw 禁止共享或重叠输入, 仅小独占构造用例; 同值载荷不保证顺序; 逻辑大小须在 int 内, 求和不溢出; 800 万预留约 288 MB |
| [leftist](<../../algorithms/数据结构/堆/左偏树/左偏树.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 20 万点混合操作与父链退化已验, 修复 Windows 递归查根栈溢出 | 逻辑编号合法; heap_add/heap_mul 后仅堆级操作, 不对受影响堆用 get_val/set_val/add_val/erase; heap_mul 正数且全部中间值不溢出; RV 注释扩展未测; 查根迭代但单次仍可能线性 |
| [dsu](<../../algorithms/数据结构/并查集/DSU.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 20 万点退化链与分块森林、多测复位已验, 查根改迭代消除递归栈依赖 | init 不超过构造容量; 根方向保持 v 的根挂到 u 的根, 非按大小合并; find 迭代路径压缩, 单次仍可能线性; 不支持删除或撤销 |
| [wdsu](<../../algorithms/数据结构/并查集/WDSU.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 归一化溢出与递归深栈已修, 独立 BFS、20 万点及跨模式容量复用已验 | 所有距离加减中间值须在 LL 内; 已连通 merge 忽略矛盾, INF 可能也是合法距离需用 same 辨别; init 不超过构造容量, 复位后旧关系失效; 仅路径压缩, 单次查根仍可能线性 |
| [monoQueue](<../../algorithms/数据结构/单调队列/单调队列.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 显式滑窗/DP 骨架, 20 万长度与平手/时机边界纳入默认对拍 | 输入含 0 号哨兵且 1-based, k>=1; 比较为固定一致顺序, 键不变; DP 仅示范指定递推, 所有加法在 LL 内; 大滑窗四种平手下标由小暴力补充, 大规模主要比最值 |
| [monoStack](<../../algorithms/数据结构/单调栈/单调栈.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 新入库的显式弹栈骨架, 四种关系与 20 万长度已接默认对拍 | 输入含 0 号哨兵且 1-based, 不存在为 0; 比较为固定一致顺序; 未把 README 的区间贡献提示实现为通用引擎, 按题改动需另验 |
| [bit](<../../algorithms/数据结构/树状数组/树状数组.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 一维及后缀版 20 万长度混合操作与多测复位已验 | 下标 1-based, 区间合法非空, init 不超过原容量; 差分取负、坐标乘积、累积与查询中间值均不溢出 LL |
| [bit2d](<../../algorithms/数据结构/树状数组/二维树状数组.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 2000×2000 在线结构化及离线任意矩形、退化行列与复位已验 | 稠密存储约 32 B/格, 合法非空矩形, init 各维不超过原容量; 全部中间值须不溢出 LL; 大规模任意更新为离线阶段, 任意在线交错由小暴力覆盖 |
| [segFhq](<../../algorithms/数据结构/树套树/线段树套FHQ_Treap.cpp>) | 还没有登记具体测了哪些行为 | 尚未发现直接关联的套件 | 不能仅凭 include 或其他模板通过就判断它已测好 |
| [st](<../../algorithms/数据结构/ST表/ST表.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 两种最值、全区间边界和 20 万点重建已验 | 非空 1-based 输入, 仅静态 max/min; 不承诺 sum 等非幂等操作或修改原数组后自动更新 |
| [skiplist](<../../algorithms/数据结构/有序表与平衡树/跳表.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 集合语义、三种 20 万插入形态及混合操作已验 | 去重集合, erase 返回 id/-1, 删除后 id 可复用; 默认 K=20 与小 K=5 旧用例, 层数须匹配规模; 键在 (-INF, INF) |
| [avl](<../../algorithms/数据结构/有序表与平衡树/AVL.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 集合语义、三种 20 万插入形态及混合操作已验 | 元素在 (-INF, INF), 排名为严格小于的数量; 随机结构内部优先级沿用时间种子, 操作流固定; 不承诺所有随机树形最坏对数 |
| [treap](<../../algorithms/数据结构/有序表与平衡树/Treap.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 集合语义、三种 20 万插入形态及混合操作已验 | 元素在 (-INF, INF), 排名为严格小于的数量; 随机结构内部优先级沿用时间种子, 操作流固定; 不承诺所有随机树形最坏对数 |
| [fhq](<../../algorithms/数据结构/有序表与平衡树/FHQ_Treap.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 集合语义、三种 20 万插入形态及混合操作已验 | 元素在 (-INF, INF), 排名为严格小于的数量; 随机结构内部优先级沿用时间种子, 操作流固定; 不承诺所有随机树形最坏对数 |
| [fhqSeq](<../../algorithms/数据结构/有序表与平衡树/FHQ_Treap_序列.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 序列双语义域及 20 万规模已验, 修复极负数最大子段和哨兵 | 区间合法非空且所有中间运算不溢出; 加乘后不查受影响最大子段和, 全覆盖恢复; 大规模变换为结构化全域, 任意区间混合由原小暴力覆盖 |
| [scapegoat](<../../algorithms/数据结构/有序表与平衡树/替罪羊树.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 集合语义、三种 20 万插入形态及混合操作已验 | 元素在 (-INF, INF), 排名为严格小于的数量; 随机结构内部优先级沿用时间种子, 操作流固定; 不承诺所有随机树形最坏对数 |
| [cartesian](<../../algorithms/数据结构/有序表与平衡树/笛卡尔树.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 双堆型/BST 映射与 20 万退化树已验 | Graph.node_cnt 不含孤点, 调用方用 ct.n; key/orig 仅最近调用 build_bst 后有效; BST 构造排序 O(n log n), 不承诺平衡, 下游递归深度单独预算 |
| [splay](<../../algorithms/数据结构/有序表与平衡树/Splay.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 集合语义、三种 20 万插入形态及混合操作已验 | 元素在 (-INF, INF), 排名为严格小于的数量; 随机结构内部优先级沿用时间种子, 操作流固定; 不承诺所有随机树形最坏对数 |
| [treeKnapDfs](<../../algorithms/动态规划/背包DP/树上背包/dfn序.cpp>) | 还没有登记具体测了哪些行为 | 尚未发现直接关联的套件 | 不能仅凭 include 或其他模板通过就判断它已测好 |
| [treeKnapBound](<../../algorithms/动态规划/背包DP/树上背包/上下界.cpp>) | 还没有登记具体测了哪些行为 | 尚未发现直接关联的套件 | 不能仅凭 include 或其他模板通过就判断它已测好 |
| [graph](<../../algorithms/图论/图的存储/Graph.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 已完成项补验: 20 万点 60 万条逻辑边, 零预留触发扩容、自环重边、LL 两端存取和清空重建 / 图论组装套件核对存图、复位与赋值, 另有独立本体套件 / 编号、邻接、度数、权值、扩容、复位与拷贝移动均有直接对拍 / 已补全局大容量对象、零边预留与连续小图复用专项 | 本套件为结构化目标规模补验, 随机/穷举语义暴力保留在原套件; 递归深链仅在独立增栈 CI 中执行, 不承诺默认栈通过; HLD 仅验证可交换点权聚合, 权和须在 LL 内 / 本套件的赋值快照不比较边权, 未直接验证 id/rev 和扩容; 这些由 graph_core_check 单独登记 / 仅测 Empty/LL 权值; 不承诺加边时已有引用有效, 不测试超出点容量的输入 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [scc](<../../algorithms/图论/连通性相关/Tarjan_SCC.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 新增 SegGraph 带权图接 SCC 的独立分量与目标规模验证 / 已完成项补验: 20 万点孤立/星/二叉/64 点环块森林, 大/单点/大 / 划分、两种缩点和带权输入都测了 / 已补全局大容量对象、零边预留与连续小图复用专项 | 本组合未测缩点 DAG 或逐个骨架点的分量编号; 分块压力非原图大深链, SCC 仍保留递归 / 本套件为结构化目标规模补验, 随机/穷举语义暴力保留在原套件; 递归深链仅在独立增栈 CI 中执行, 不承诺默认栈通过; HLD 仅验证可交换点权聚合, 权和须在 LL 内 / 未测完整 SegGraph 应用流水线; 深链只到 2000 点 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [ebcc](<../../algorithms/图论/连通性相关/Tarjan_EBCC.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 已完成项补验: 20 万点孤立/星/二叉/64 点环块森林, 大/单点/大 / 桥编号、边双、桥森林、重边和自环都测了 / 已补全局大容量对象、零边预留与连续小图复用专项 | 本套件为结构化目标规模补验, 随机/穷举语义暴力保留在原套件; 递归深链仅在独立增栈 CI 中执行, 不承诺默认栈通过; HLD 仅验证可交换点权聚合, 权和须在 LL 内 / 深链只到 2000 点, 更长链仍受递归栈限制 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [vbcc](<../../algorithms/图论/连通性相关/Tarjan_VBCC.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 已完成项补验: 20 万点孤立/星/二叉/64 点环块森林, 全块和割点核对, 大/单点/大 / 点双、割点、指定根、反查和圆方森林都测了 / 已补全局大容量对象、零边预留与连续小图复用专项 | 本套件为结构化目标规模补验, 随机/穷举语义暴力保留在原套件; 递归深链仅在独立增栈 CI 中执行, 不承诺默认栈通过; HLD 仅验证可交换点权聚合, 权和须在 LL 内 / 不接收自环; 深链只到 2000 点 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [bct](<../../algorithms/图论/连通性相关/圆方树/BCT.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 已完成项补验: 20 万点孤立/星/二叉/64 点环块森林, 全块/割点/邻接核对和大小复用 / 独立 BCT 已对拍, 与 VBCC 共存和复位隔离也测了 / 已补全局大容量对象、零边预留与连续小图复用专项 | 本套件为结构化目标规模补验, 随机/穷举语义暴力保留在原套件; 递归深链仅在独立增栈 CI 中执行, 不承诺默认栈通过; HLD 仅验证可交换点权聚合, 权和须在 LL 内 / 不接收自环; 深链只到 2000 点 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [lca](<../../algorithms/图论/树上问题/最近公共祖先/DFN_LCA.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 带权全点对、跳转、成员表与虚树双实现组装已测, 默认压力到 20 万点 / 无权森林的单点对/点集 LCA、距离、跳转和子树大小已对拍 / 已补全局大容量对象、零边预留与连续小图复用专项 | 保留递归; 深链不在本套件无参数回归中, 独立 CI stress 作业记录栈探针和增栈百万点结果, 不承诺默认栈通过; 未逐项核对完整 rmq/fa 倍增表 / 本套件仅测无权小森林, 带权/空态/大规模见 lca_vt_stress_check; 完整 rmq/fa 表未逐项核对 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [hldlca](<../../algorithms/图论/树上问题/最近公共祖先/HLD_LCA.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 带权全点对、任意根、成员表与虚树双实现组装已测, 默认压力到 20 万点 / 默认根无权森林的单点对/点集 LCA 与距离已对拍 / 已补全局大容量对象、零边预留与连续小图复用专项 | 保留递归, 默认回归不跑大深链; 默认栈限制由独立 CI stress 作业另列探针结果, 不承诺深链通过; 指定根只测连通树, son/top 未逐项核对 / 本套件仅测默认根无权小森林, 指定根/带权/空态/大规模见 lca_vt_stress_check; son/top 未逐项核对 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [binliftLca](<../../algorithms/图论/树上问题/最近公共祖先/树上倍增LCA.cpp>) | 还没有登记具体测了哪些行为 | 尚未发现直接关联的套件 | 不能仅凭 include 或其他模板通过就判断它已测好 |
| [eulerLca](<../../algorithms/图论/树上问题/最近公共祖先/欧拉序LCA.cpp>) | 还没有登记具体测了哪些行为 | 尚未发现直接关联的套件 | 不能仅凭 include 或其他模板通过就判断它已测好 |
| [tarjanLca](<../../algorithms/图论/树上问题/最近公共祖先/tarjan.cpp>) | 还没有登记具体测了哪些行为 | 尚未发现直接关联的套件 | 不能仅凭 include 或其他模板通过就判断它已测好 |
| [hld](<../../algorithms/图论/树上问题/树链剖分/HLD.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 已完成项补验: 400 组任意根小树每组 160 操作, len/sum/max 同查; 20 万点二叉树每轮 10 万操作, 大/单点/大; 空森林及 BCT 组装 / 单树与森林的点权路径/子树加和查询及多测复位已对拍 / 已补全局大容量对象、零边预留与连续小图复用专项 | 本套件为结构化目标规模补验, 随机/穷举语义暴力保留在原套件; 递归深链仅在独立增栈 CI 中执行, 不承诺默认栈通过; HLD 仅验证可交换点权聚合, 权和须在 LL 内 / 仅测加和代数, 指定根仅测 1, 查询只核对 sum; 未直接测其他代数、边权映射、任意根、空森林或长于 61 的深链, 剖链成员表未逐项核对 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [virtualTree](<../../algorithms/图论/树上问题/虚树/二次排序.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 两份 LCA 组装及独立删枝参照已测, 全点/重复输入压力到 20 万点 / 两版虚树的压缩边与权值、任意必选 root、重复点、跨树清空和复用已对拍 / 已补全局大容量对象、零边预留与连续小图复用专项 | 默认结构化压力不含大深链, LCA 递归受栈限制; 百万点及增栈深链由独立 CI stress 作业自动执行并另出报告, 不冒充本套件无参数回归范围 / 本套件仅搭配 DFN_LCA 且最多 64 点, HLD_LCA 与大规模见 lca_vt_stress_check; 空集不保留 root, 单点无边且不进入 Graph.used, 不提供独立虚树点集或重定根结果 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [virtualTreeStack](<../../algorithms/图论/树上问题/虚树/单调栈.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 两份 LCA 组装及独立删枝参照已测, 全点/重复输入压力到 20 万点 / 两版虚树的压缩边与权值、任意必选 root、重复点、跨树清空和复用已对拍 / 已补全局大容量对象、零边预留与连续小图复用专项 | 默认结构化压力不含大深链, LCA 递归受栈限制; 百万点及增栈深链由独立 CI stress 作业自动执行并另出报告, 不冒充本套件无参数回归范围 / 本套件仅搭配 DFN_LCA 且最多 64 点, HLD_LCA 与大规模见 lca_vt_stress_check; 空集不保留 root, 单点无边且不进入 Graph.used, 栈工作状态未逐项核对 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [diameterDfs](<../../algorithms/图论/树上问题/树的直径/两次dfs.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 已完成项补验: 20 万点星/64 点长臂, 零权和 1e12 边权, 大/单点/两点/大, 根分量约定 / 非负权直径长度、端点和路径已对拍, 无权长度另行核对 / 已补全局大容量对象、零边预留与连续小图复用专项 | 本套件为结构化目标规模补验, 随机/穷举语义暴力保留在原套件; 递归深链仅在独立增栈 CI 中执行, 不承诺默认栈通过; HLD 仅验证可交换点权聚合, 权和须在 LL 内 / 未直接测森林、深链或大规模; 无权路径和完整 pre 表未逐项检查; 负边权不在契约内 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [diameterDp](<../../algorithms/图论/树上问题/树的直径/树形dp法.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 已完成项补验: 20 万点星/64 点长臂, 零权、正负 1e12 边权, 大小交替和根分量约定 / 有权及无权直径长度已对拍, 包含负边权 / 已补全局大容量对象、零边预留与连续小图复用专项 | 本套件为结构化目标规模补验, 随机/穷举语义暴力保留在原套件; 递归深链仅在独立增栈 CI 中执行, 不承诺默认栈通过; HLD 仅验证可交换点权聚合, 权和须在 LL 内 / 未直接测森林、深链、大规模或固定全负树; down 表未逐项核对; 不承诺非空边路径 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [centroid](<../../algorithms/图论/树上问题/树的重心/树的重心.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 已完成项补验: 20 万点星/64 点长臂, 全负/零/1e12 点权, 大/单点/两点/大 / 正/零/负点权的最优值和全部答案已用删点暴力核对 / 已补全局大容量对象、零边预留与连续小图复用专项 | 本套件为结构化目标规模补验, 随机/穷举语义暴力保留在原套件; 递归深链仅在独立增栈 CI 中执行, 不承诺默认栈通过; HLD 仅验证可交换点权聚合, 权和须在 LL 内 / 仅支持无向连通树, 未测深链、大规模或权和极值; sz 表未逐项核对; 单点删完约定为 0 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [center](<../../algorithms/图论/树上问题/树的中心/树的中心.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 已完成项补验: 20 万点星/64 点长臂, 全零及 1e12 边权, 全部中心及全部 ecc, 大小交替 / 无权/非负权顶点中心、半径、每点最远距离和直径端点均已对拍 / 已补全局大容量对象、零边预留与连续小图复用专项 | 本套件为结构化目标规模补验, 随机/穷举语义暴力保留在原套件; 递归深链仅在独立增栈 CI 中执行, 不承诺默认栈通过; HLD 仅验证可交换点权聚合, 权和须在 LL 内 / 仅支持无向连通树及 Empty/非负整数权, 不求边内部中心; 深链测至 2000 点, 路径权和须在 LL 内 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [eulerUndirected](<../../algorithms/图论/欧拉图/无向图欧拉边路径.cpp>) | 还没有登记具体测了哪些行为 | 尚未发现直接关联的套件 | 不能仅凭 include 或其他模板通过就判断它已测好 |
| [eulerDirected](<../../algorithms/图论/欧拉图/有向图欧拉边路径.cpp>) | 还没有登记具体测了哪些行为 | 尚未发现直接关联的套件 | 不能仅凭 include 或其他模板通过就判断它已测好 |
| [eulerPointUndirected](<../../algorithms/图论/欧拉图/无向图欧拉点路径.cpp>) | 还没有登记具体测了哪些行为 | 尚未发现直接关联的套件 | 不能仅凭 include 或其他模板通过就判断它已测好 |
| [eulerPointDirected](<../../algorithms/图论/欧拉图/有向图欧拉点路径.cpp>) | 还没有登记具体测了哪些行为 | 尚未发现直接关联的套件 | 不能仅凭 include 或其他模板通过就判断它已测好 |
| [topo](<../../algorithms/图论/拓扑排序/拓扑排序.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 已完成项补验: 20 万点 DAG、重复边、加回边成环, 大/空/单点/大复用 / 判环、合法拓扑序、原图入度保护及 852 万容量下 1000 组小测已验证 / 已补全局大容量对象、零边预留与连续小图复用专项 | 本套件为结构化目标规模补验, 随机/穷举语义暴力保留在原套件; 递归深链仅在独立增栈 CI 中执行, 不承诺默认栈通过; HLD 仅验证可交换点权聚合, 权和须在 LL 内 / 本套件大容量用例的实际点数不超过 240, 20 万实际点数见 completed_graph_stress_check; 不要求字典序最小, 不验证失败时部分序列的具体顺序 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [segGraph](<../../algorithms/图论/优化建图/线段树优化建图.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 六类连边、独立全点对及可达性、SCC 组合和默认 20 万规模已验; 原点与中继独立预算 / 已补全局大容量对象、零边预留与连续小图复用专项 | 只测合法非空原点区间, build(0) 不支持; 中继数不得超预算, 重建后旧编号失效; Dijkstra 距离须小于 INF 且加法不溢出; 泛型权仅测 LL/Empty / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [treeGraph](<../../algorithms/图论/优化建图/树上倍增优化建图.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 树路径最多4段、路径间最多8边; 三种边权独立暴力、百万操作与20万森林及容量复用已覆盖 / 已补全局大容量对象、零边预留与连续小图复用专项 | 静态已建表森林, 外置提供者须有根深度1、fa[k][u]及断连返回-1的lca; 路径端点限原点, 中继不超预算; 只保证可达性和权值路径语义, 不用于计数; 定位O(T+log n)而非O(1); 深链采用闭式提供者, 不改变或覆盖库内LCA递归栈限制; 距离数值限制由下游决定 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [dij](<../../algorithms/图论/最短路问题/单源最短路径/dijkstra_heap.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 已验证 SegGraph 非负权单源组合, 包含中继源、不可达和目标规模 / 独立Floyd全源与多源，20万规模/重边/重复源复位 / 已补全局大容量对象、零边预留与连续小图复用专项 | 仅登记 SegGraph 组合, 不代表 Dijkstra 全接口最终验收; 未测多源、负权不适用, 合法距离需小于 INF / n>=1 且在构造容量内，每次run前init；有限距离<INF，候选加法在LL内；边权非负且<INF / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [dijN](<../../algorithms/图论/最短路问题/单源最短路径/dijkstra.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 独立Floyd全源与1800稠密/逆链复位 / 已补全局大容量对象、零边预留与连续小图复用专项 | n>=1 且在构造容量内，每次run前init；有限距离<INF，候选加法在LL内；边权非负且<INF / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [spfa](<../../algorithms/图论/最短路问题/单源最短路径/spfa 存图.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 独立Floyd全源，20万容量/负链与1800退化，隔离不可达负环 / 已补全局大容量对象、零边预留与连续小图复用专项 | n>=1 且在构造容量内，每次run前init；有限距离<INF，候选加法在LL内；允许负边但源点可达部分不得有负环；SPFA/BF最坏二次或更慢，20万顺序友好图通过不代表任意大图可承受 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [bf](<../../algorithms/图论/最短路问题/单源最短路径/Bellman-Ford存图.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 独立Floyd全源，20万容量/负链与1800退化，隔离不可达负环 / 已补全局大容量对象、零边预留与连续小图复用专项 | n>=1 且在构造容量内，每次run前init；有限距离<INF，候选加法在LL内；允许负边但源点可达部分不得有负环；SPFA/BF最坏二次或更慢，20万顺序友好图通过不代表任意大图可承受 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [bfRing](<../../algorithms/图论/最短路问题/判断负环/bf_-ring.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 独立Floyd判环、跨分量/复位/大权溢出反例、20万容量与1800退化 / 已补全局大容量对象、零边预留与连续小图复用专项 | n>=1 且在构造容量内，Graph 的有符号整数边权至多 LL；dist 是 i128 判环工作量，不能当成普通最短距离；BF 最坏 O(n(n+m))、SPFA 最坏 O(nm+n)，20万顺序友好图通过不代表任意大图可承受 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [spfaRing](<../../algorithms/图论/最短路问题/判断负环/spfa_-ring.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 独立Floyd判环、跨分量/复位/大权溢出反例、20万容量与1800退化 / 已补全局大容量对象、零边预留与连续小图复用专项 | n>=1 且在构造容量内，Graph 的有符号整数边权至多 LL；dist 是 i128 判环工作量，不能当成普通最短距离；BF 最坏 O(n(n+m))、SPFA 最坏 O(nm+n)，20万顺序友好图通过不代表任意大图可承受 / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [floyd](<../../algorithms/图论/最短路问题/全源最短路径/floyd.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 旧矩阵例程仅清本轮方阵, 非负图与大小复用已补验 | 保留旧 standalone main 和 N=110 的固定矩阵, 1<=n<110; 仅非负 int 小权值, 不覆盖负边/负环/大权溢出; 不代表已完成现代 Graph 引擎整理 |
| [kruskal](<../../algorithms/图论/生成树问题/最小生成树/kruskal/Kruskal.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 独立枚举森林，负权/断连/LL极值；20万Kruskal与2000稠密双算法，返回边及复位 / 已补全局大容量对象、零边预留与连续小图复用专项 | n>=1，成对半边的无向Graph且权为int/LL；总权每次累加在LL内；不承诺同权时两算法选同一棵树；Graph clear后旧边编号失效；Prim为O(n²+m)朴素版，20万规模只验Kruskal / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [prim](<../../algorithms/图论/生成树问题/最小生成树/prim/Prim.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 独立枚举森林，负权/断连/LL极值；20万Kruskal与2000稠密双算法，返回边及复位 / 已补全局大容量对象、零边预留与连续小图复用专项 | n>=1，成对半边的无向Graph且权为int/LL；总权每次累加在LL内；不承诺同权时两算法选同一棵树；Graph clear后旧边编号失效；Prim为O(n²+m)朴素版，20万规模只验Kruskal / 只检验所在分组的容量复用与闭式夹具, 不代替完整语义/大规模性能验收; 递归仅浅树; 高位哨兵只检查代码中列出的工作表, 构造容量与边数组峰值仍占内存 |
| [trie](<../../algorithms/字符串/字典树/字典树.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 普通字符串计数与非负整数最大异或; 20万长串/整数及多测复用 / 普通Trie包含根的预算、重复插入和300轮清空重建 | 合法字符集; 字符串和非负 LL 整数接口不混用; 单对象/版本元素数<=INT_MAX; max_nodes 足够且索引能放入 int; clear 保留容量但旧结点/版本句柄失效; 不支持删除 |
| [persistentTrie](<../../algorithms/字符串/字典树/可持久化字典树.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 任意历史分叉、树路径根差、前缀/LCP与异或第k大; 20万规模和超int计数已测 / 全部64种HB的精确预算, 空串也复制根, 空哨兵不占cap | 合法字符集; 字符串和非负 LL 整数接口不混用; 单对象/版本元素数<=INT_MAX; max_nodes 足够且索引能放入 int; clear 保留容量但旧结点/版本句柄失效; 不支持删除; HB在0..63且所有整数和查询值放进HB+1位; 根差按每个值的重复次数非负; 多根正/负侧计数和分别<=LLONG_MAX; xs.size()<=INT_MAX |
| [mint](<../../algorithms/数学/数论/mint.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 15种模数边界与20万运算/输入/表重建; 修复最小i128负幂溢出、constexpr除法及反向整数比较 | 模数编译期固定且1<MOD<2^62; 不支持u128/浮点构造; 求逆、除数、负幂底数须可逆; 输入为合法十进制; 素数阶乘n<MOD且查询已预处理; 合数comb单次约O(k log²n), 不承诺高频大k性能; 不是Lucas或动态模数组件 |
| [eulerSieve](<../../algorithms/数学/数论/质数筛/欧拉筛.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 欧拉线性筛、最小质因子与快速分解; 千万表全量核验和跨规模重建 | 0<=n<INT_MAX且内存足够; 查询0..n, 分解1..n; init全量重建并保留容量; 不内置phi/mu; primes下标0起 |
| [eratosthenes](<../../algorithms/数学/数论/质数筛/埃氏筛.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 位压缩埃氏筛, 千万表逐点判素及质数顺序核验 | 0<=n<INT_MAX且内存足够, 查询0..n; vector<bool>位压缩, 不提供最小质因子; init全量重建且保留容量; primes下标0起 |
| [segSieve](<../../algorithms/数学/数论/质数筛/区间筛质数.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 百万宽区间与近1e12平方边界, 位压缩并复用基础筛 / 小区间全量试除, 负端点截断和空区间 | 返回闭区间全部质数; l先截到2, 空区间直接返回; sqrt(r)<INT_MAX且基础表和区间表均可存, 仍须筛到sqrt(r), 不面向LLONG_MAX附近区间 |
| [divisorSums](<../../algorithms/数学/数论/约数/约数个数及约数和公式法.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 20万约数答案和int上界; 返回值隔离, 不再覆盖全局变量 | 只接正int, 约数和LL/个数int; O(sqrt x)用于少量输入, 非64位分解器; 旧全局cnt/sum调用改为结构化绑定返回值 |
| [bigint](<../../algorithms/数学/高精度计算/高精度.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 修复商前导零和最小i128转换; 全接口原生/竖式对拍及十万位复位, 更大运算有独立Python验收 | 只接受合法十进制; 原生构造为<=64位整数或有符号i128, 不接u128; 除数非零、sqrt非负、pow指数非负、mod模数为正LL、窄化结果须在范围内; 十万位乘除开方与100000!的独立答案另见专项深测, 不承诺所有评测环境耗时 |
| [lucas](<../../algorithms/数学/组合数学/卢卡斯定理求组合数.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 修复模 2 初始化死循环, 20 万素数表及 20 万查询已验; 复用阶乘表并提供完整 Usage | p>=2 为调用方保证的 int 素数且 O(p) 表能放下; 0<=n<=LLONG_MAX; 非扩展 Lucas, 不适合 1e9 级模数整表; 原地重建覆盖旧表 |
| [binomLoop](<../../algorithms/数学/组合数学/循环求组合数.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 精确小数、LL 上界与 20 万循环独立对拍; 大 n 小 k 无需建到 n, 中间乘法用 i128 | n>=0, p 为调用方保证的素数且 2<=p<=LLONG_MAX; 有效 k 的 min(k,n-k)<p; O(k+log p) 单次, 无预处理; 不是任意合数模或跨分母 p 因子的算法 |
| [binomPascal](<../../algorithms/数学/组合数学/打表杨辉三角求组合数.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 合数/模 1 与 LL 上界已验, 2000 规模跨模重建; 下三角存储和 O(1) 查询 | 0<=n<INT_MAX, 1<=mod<=LLONG_MAX, 实际规模须容纳 O(n²) 时间/空间; 查询 n 在已建范围内; c[n] 只有 0..n |
| [binomFactInv](<../../algorithms/数学/组合数学/阶乘表及阶乘逆元表求组合数.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 素数阶乘与 500 万表逐项验收, 多测可换模; Lucas 复用此表, 固定模数与 mint 功能重合 | 调用方保证 p 为素数, 0<=n<p 且 n<INT_MAX, 查询 n 已预处理; 16(n+1) B 有效表数据, 缩表保留容量; 固定模数可用 mint, 本件不做 Lucas 自动路由 |
| [utils](<../../algorithms/杂项/utils/utils.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 别名/常量/方向与 20 万填充边界已验, fast_io 改 inline, n+10 在 size_t 域计算 / LOCAL 调试宏已用真实 cerr 输出核验 / 整数二分独立核验四十万量级平方边界及LL上界；全符号组合和LL极值, 20万随机除法; 明确不可表示商的契约 | z_fill_n 保持从 0 填 min(n+10,size) 个元素, 不扩容也不清远端尾部; 每个容器 size>=n>=0; fast_io 在首次 IO 前调用且不混用其他标准流 IO; endl 宏不刷新 / 仅 LOCAL 分支定义调试宏; debug_array 按 1..n 且调用方保证容量, 保留现有语句宏使用方式 / 输入LL, x<=0返回0; 固定位宽中浮点估计后整数修正; ceil结果可到3037000500；b!=0且(a,b)!=(LLONG_MIN,-1), 因其数学结果不在LL内; 不用返回哨兵伪装无效除法 |
| [rw](<../../algorithms/杂项/快读快写/快读快写.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 修复超长浮点输入越界、最大double截断及空白/bool支持; 百万整数与4MiB跨界逐字节通过 / 共享杂项组装与原有快读写冒烟保留, 专项深测另有独立套件 | 合法空白分隔token, 整数在目标类型内, bool使用0/1; double读十进制/科学计数法, 写固定六位而非无损往返; 不与其他标准流读写混用, 不作为交互输入方案; 正常退出析构刷写由check_bigint_rw.py外部子进程另验 / 共享杂项套件仅保留基础冒烟; 完整类型/浮点/百万输入范围看rw_check, 退出析构由外部深测另验 |
| [i128](<../../algorithms/杂项/128位整数/128int.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 修复 -2^127 读入溢出, 独立字符串答案与 20 万值流读写已验 | 合法十进制 token 且值在 i128 内, 不模拟标准流的十六进制/格式宽度; u128 仅别名不提供本文件流重载, 可用 rw; IO 不是对所有算术的溢出保护 |
| [rnd](<../../algorithms/杂项/随机数/z_rnd.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 补 bool/字符分布与浮点宽度溢出, 可传固定引擎, 完整类型边界和百万分布冒烟已验 | 整数至多 64 位; 浮点端点有限且 lo<=hi, 相等时返回端点; 离散浮点近似均匀, 测试不是统计证明; MT 非安全随机源, 不给 SipHash 生成密钥; 跨标准库不承诺分布序列一致 |
| [customHash](<../../algorithms/杂项/防卡/哈希防卡.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 升级作者保守版 SipHash-4-8, 独立参考核验与 20 万容器操作; 原整数调用仍可用, 新增字符串/整数对 / 既有 10 万整数哈希容器组装回归保留 | 64 位目标; 防护依赖默认 random_device 的熵与密钥保密, 固定密钥仅对拍; 不承诺无碰撞或 unordered_map 最坏 O(1); 桶阈值只约束固定夹具, 有限测试不能证明密码安全 / 共享 misc 仅功能冒烟; 不把桶统计当安全证明, 密钥和独立参考见 hash_check |
| [discrete](<../../algorithms/杂项/离散化/离散化.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 20 万多形态及字符串/整数对已验, 补 clear 和排名有效性契约 / 既有离散化小规模组装回归保留 | 索引 1-based, 输入不自动丢弃第 0 项; size/逆映射须已 build, 追加后旧排名失效; < 和 == 一致, 浮点不含 NaN, 不同值数<=INT_MAX; 不直接修改 v 后沿用旧状态 / 共享 misc 仅基础组装回归, 完整复用与规模看 tool_core_check |
| [misraGries](<../../algorithms/杂项/主元素问题/Misra-Gries.cpp>) | 登记的测试都已通过 (Windows 普通回归) | 小域穷举及 20 万小 k 已验, 加守卫与完整题目用法, 明确二次计数和复杂度 | 返回无序不同值, 严格次数>floor(n/k); k<2 返回空, 全部输入元素参与; O(n min(k,n)) 时间, 适合小 k; 改成单遍流式不能省掉本函数二次核验 |
