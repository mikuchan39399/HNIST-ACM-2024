# 字符串路线图

[全部方向](README.md) · [设计分类](../features/design/字符串.md) · [个人进度](../progress/字符串.md)

本页保存推荐路径、家族导航和待建目标，不进入纸质手册；推荐顺序不代替当前工作板，也不代表本人已学过。目录位置是源码定位，设计类型看分类表。

<a id="route-4"></a>
## 方向总览

已有: 字典树×2(含可持久化)。这是全库最明显的短板之一, 按顺序补:

先学(高频):
字符串哈希\ —— 双模哈希加子串比较, 半天搞定, 回报最高
KMP\ —— 然后顺手学 Z函数\(扩展 KMP)和最小表示法
AC自动机\ —— 字典树加失配边, 学完字典树就能学

再学(中频):
Manacher\ —— 回文半径
后缀数组\ —— SA、height 数组、LCP 配 RMQ

最后:
后缀自动机\ 和 回文自动机\ —— SAM/PAM 都是大工程, 按 §3.5 第三档写自含式
序列自动机\ —— 子序列自动机, 很小, 半小时的事, 顺手写掉

不学: 后缀树、Lyndon 分解, 射程外。
入库流程按 [当前模板契约](../../rule.md#9-验证标准) 执行。

## 原占位目录

这些旧 README 只有相同的空壳文字，现合并为索引。它们不是实现或使用说明；其中已有源码/笔记的目录也曾带着过时空壳，实际资产以 catalog 为准。

| 目录位置 | 原文身份 |
|---|---|
| <a id="route-1"></a>AC自动机 | 原骨架占位，目标保留在进度表 |
| <a id="route-2"></a>KMP | 原骨架占位，目标保留在进度表 |
| <a id="route-3"></a>Manacher | 原骨架占位，目标保留在进度表 |
| <a id="route-5"></a>Z函数 | 原骨架占位，目标保留在进度表 |
| <a id="route-6"></a>后缀数组 | 原骨架占位，目标保留在进度表 |
| <a id="route-7"></a>字典树 | 原骨架占位，目标保留在进度表 |
| <a id="route-8"></a>字符串哈希 | 原骨架占位，目标保留在进度表 |
