# 连通性四件套验证与注释整理

2026/9/6，astra；用户授权补强对拍并改为赛场接口注释。本轮未提交。

| 实现 | 小图验证范围 |
|---|---|
| SCC | 1775 图：n≤3 有向图全枚举（含自环），结构样例，三种固定种子各 400 图；Warshall 划分、计数、拓扑编号、保留重边/去重 DAG、带权输入 |
| EBCC | 2346 图：n≤5 无向简单图全枚举，重边随机/结构样例，另加两组自环；逐边删除判桥、精确偶数半边号、删桥划分、成员表及桥森林完整边集 |
| Tarjan_VBCC | 2344 图：逐点删除判割点、枚举极大点双集合、孤立点、root=1/n 只扫对应连通块、圆方森林邻接与两个反查接口 |
| BCT.cpp 内的 VBCC | 独立编译运行同一套暴力与边界；两种 include 顺序分别选中实现，不再将守卫跳过误当副本行为验证 |

每个套件另外验证 20 组容量/复位组合：N=2000，n 按 2000→1→2000→2→2000，分别造空图、链、星、环；断言分量/桥/割点/圆方森林规模与成员次数，且原图和内部图的边容量未增长。另测 20 点、190 边的 DAG，构造 M 恰好等于输出边数。无向容量测试使用带权 Graph，验证 EBCC/VBCC 忽略边权。

- Windows GCC 15.2，C++20、O2、Wall/Wextra/Werror：统一入口 `./scripts/run_checks.ps1 -Filter conn`，2/2 通过。日志 `.zoi-checks/d4e773aa006644fca0b9797e120edbdb/logs`。
- WSL Ubuntu GCC 13.1，C++20、O1、Wall/Wextra/Werror、ASan/UBSan、标准库断言、泄漏检查：2/2 通过。命令与输出 `.zoi-checks/conn-review/linux.sh`、`linux-final.log`。
- ABI 实测 int=4、vector<int>=24、无权半边=8 字节；用于构造注释的基础预留估算，成员表按实际 capacity 另算，不将递归栈或原图算入算法器。
- 四个引擎去掉注释后与 HEAD 的 token 完全一致。本轮未发现算法反例，未修改实现。

修正的使用契约：EBCC 返回原图偶数半边号，并不保证 DFS 方向；孤立点也有方点，圆方森林最多 2n 点；缩点/建树接口追加结果，重建需 clear。保留外部图注入、带权输入/无权输出、编号与反查前提；删除 BCT 装饰性标题，保留组装提示。rule.md 同步两处错误口径。

边界：2000 点递归测试不保证更长链在任意评测栈上安全；按既定规则保持递归。小图枚举和随机样本不等于所有规模的正确性证明。本轮未做 SegGraph→SCC 的完整应用流水线，只测其所用带权 Graph 接口。未推送，旧 CI 绿灯不覆盖本轮。

用户同时确认撤下 CF786B_Legacy.cpp：删除失效豁免、更新资产表，个人学习记录保留为待确认并标注源码撤下。四件套学习状态不因对拍通过改变。


## Usage 补齐

2026/9/6 按用户要求将四份 Usage 改为完整 main() , 输入 n / m 后构造 , 覆盖 SCC 划分与拓扑遍历 , EBCC 桥端点 , VBCC 成员与反查 , BCT 圆方森林输出. 去除省略号和未定义 N / M , 四份提取示例均通过 GCC 15.2 C++20 / O2 / Wall / Wextra / Werror 编译 , 各运行单点与三角形带桥两组输入. 证据位于 .zoi-checks/conn-review/usage . 四份注释改用英文半角标点与间隔 , 格式偏好写入 rule.md . 引擎实现未变 , 未重复运行算法回归.


## BCT 独立类型

2026/9/6 用户要求 BCT 独立. BCT.cpp 改为 struct BCT 和 Z_OI_BCT 守卫 , 自带 Tarjan 实现 , 不引用 VBCC , Usage 改为 BCT bct(n). build / build_tree 等接口及 vbcc_cnt / vbcc_cir 结果名保持原语义. 旧代码若仅包含 bct.h 却声明 VBCC , 需改为 BCT.

两种包含顺序均实例化 BCT 与 VBCC , 检查结果及 init 的实例隔离 ; 共享暴力通过 CheckedBlock 分别验证两类型. Windows GCC 15.2 两套完整回归通过 , 日志 .zoi-checks/263781f71b4243f3aea5e19c177e52c6/logs ; Linux GCC 13.1 ASan/UBSan 两套通过 , 日志 .zoi-checks/conn-review/bct-split-linux.log . 新 Usage 提取编译与两组输入通过 , 两个生成表 -Check 通过. 未提交推送.
