# 库减负实施完成

范围：`F:\c++\my_code\HNIST-ACM-2024` 的测试资产导航与检查入口。算法模板未改，未提交或推送 Git。

## 使用上的变化

`rules/reliability.md` 已变为生成物，不再手工维护等级。新增/删除资产、catalog 或测试 include 关系后运行：

```powershell
./scripts/make_reliability.ps1
```

只检查是否过期：

```powershell
./scripts/make_reliability.ps1 -Check
```

CI 运行第二条命令；过期会失败但不改文件。普通对拍不改总览，单纯修改算法正文也无需重新生成。

## 架构结果

- `check_inventory.ps1` 共用校验和资产关系；runner、总览、TEST GAP 不再各自维护名单。
- `make_reliability.ps1` 稳定生成文件链接与测试引用，无时间戳、机器绝对路径或历史 PASS 推断。
- 106 项资产全部列出：51 个带直接引用的引擎、22 个未发现直接引用的引擎、17 个笔记、16 个豁免 C++；另列 18 个对拍文件。语法检查仍选择原来的 89 个源文件。
- A/B/C 是测试资产导航，不表示行为覆盖、独立暴力或当前版本运行通过。
- 旧手工表已逐字完整归档到 sweep-history；历史备注没有丢失。六个规则分卷未增加，手工同步义务已从 rule 中移除。

## 验证

Windows PS5.1 和 Linux PS7.4.13 均通过资产自检 4/4 组（每环境 11 次生成器调用）、原检查入口自检 9/9；Linux 真实 number_boundary 套件集成运行 1/1 通过。Windows 生成的总览在 Linux 上 `-Check` 一致。

覆盖引用增删、路径别名和同名文件、注释/原始字符串/续行、跳板定位、笔记与豁免、生成幂等、只读过期检查、非法 catalog 保留原报告，以及原入口的编译错误/断言/超时等失败路径。

actionlint 与 `git diff --check` 通过。日志在仓库 `.zoi-checks/inventory-*`，真实套件报告在 `.ci-results/inventory-probe/summary.json`。本次没有重跑无关算法全库；尚无未推送版本的线上 CI 结果。

状态板已解除领取并留给 GLM 交接说明，完整证据已移档。
