矩阵树定理 (Matrix-Tree / Kirchhoff)

无向图生成树个数等于基尔霍夫矩阵 G = D - A 的任意一个 n-1 阶主子式的行列式; D 是度矩阵, A 是邻接矩阵; 自环不计入度数, 重边按条数计入邻接。

有向图: 以 r 为根的外向生成树个数 = G = Dout - A 去掉第 r 行第 r 列后的行列式; 内向树把出度矩阵换成入度矩阵。

实现: 取模意义下高斯消元求行列式 O(n^3); 无模数且边权为整数时可用 double 消元数点数, 注意行交换变号。
